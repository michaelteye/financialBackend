export enum PaticipationTypes {
  GENERAL = 'general',
  CUSTOM = 'custom',
}

export enum GoalNature {
  EMERGENCY = 'emergency',
  ROTATIONAL = 'rotational',
  DEFAULT = 'default',
  SPLIT_AND_SHARE = 'splitandshare',
}
